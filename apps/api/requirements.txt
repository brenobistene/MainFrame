fastapi>=0.110
uvicorn[standard]>=0.27
google-api-python-client>=2.120
google-auth>=2.29
google-auth-oauthlib>=1.2
python-dotenv>=1.0
# Provê IANA tz database em Python puro — necessário no Windows, onde
# `zoneinfo` da stdlib não acha "America/Sao_Paulo" no SO. No-op em
# Linux/macOS (já têm tzinfo do sistema, mas funciona como fallback).
tzdata>=2024.1
# Necessário pro FastAPI processar multipart/form-data (upload de CSV no
# Hub Finance — endpoint /api/finance/import/nubank-csv).
python-multipart>=0.0.7
# Schema migrations. Projeto usa sqlite3 puro (sem SQLAlchemy ORM), então
# migrations são escritas em SQL raw via op.execute(). Baseline em
# alembic/versions/0001_baseline.py marca o estado atual do DB criado por
# init_db(); novas migrations daqui em diante. Ver alembic/README.md.
alembic>=1.13
# Test stack — pytest + httpx (TestClient do FastAPI usa httpx por baixo,
# pra rodar requests sem subir uvicorn). pytest-cov pra coverage report.
pytest>=8.0
httpx>=0.27
pytest-cov>=5.0
